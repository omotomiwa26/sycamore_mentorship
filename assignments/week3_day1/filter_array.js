// Use filter() to create a new array from [10, 15, 20, 25, 30] that only contains numbers greater than 20.

const numbers = [10, 15, 20, 25, 30];
const numbers_greater_than_20 = numbers.filter(number => number > 20);
console.log(numbers_greater_than_20);

