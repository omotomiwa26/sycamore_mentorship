// Use map() to create a new array where each number from the array [1, 2, 3, 4, 5] is multiplied by 3.

const numbers = [1, 2, 3, 4, 5];
const tripled_numbers = numbers.map(number => number * 3);
console.log(tripled_numbers);